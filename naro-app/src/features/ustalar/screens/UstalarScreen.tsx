import { Wrench } from "lucide-react-native";

import { PlaceholderFlow } from "@/shared/components/PlaceholderFlow";

export function UstalarScreen() {
  return (
    <PlaceholderFlow
      icon={Wrench}
      title="Ustalar"
      description="Bölgendeki servis sağlayıcıları burada listelenecek; değerlendirme ve mesafeye göre sıralanır."
    />
  );
}
